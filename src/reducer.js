
export default function reducer(state, action) {
    switch(action.type) {
        case "isIdle":
            return 
    }
}

export  const initialState = {
    loading: false,
    idle: false,
    error: false,
    data: []
}